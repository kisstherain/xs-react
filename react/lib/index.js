import ReactMount from './mount';
import ReactConstant from './constant';
import ReactElement from './element'
import ReactComponent from './component';

//输出所有类，用于外部继承替换
module.exports = {
	ReactMount,
	ReactConstant,
	ReactElement,
	ReactComponent,
}
